type ContactUsModel = {
  icon?: string;
  link?: string;
  type?: string;
  value?: string;
  iFrameList?: string[];
  imgSource?: any; // Accepts require() for local images or a URI for remote images
};
