// 1) If you want an array of “objects” (e.g. for a dropdown), you can do:
export type SchedulerContactTypeModel = {
  label?: string; // "Lead" | "Prospect" | "Client"
  Id?: number; // 1 | 2 | 3
};
